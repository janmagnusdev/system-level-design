#include "adder.h"

/* ----- process body ----- */
void
adder::add()
{
  while( true )
  {
    	/* ----- main loop ----- */

  	/* ----- reset block ----- */
    	/* ----- calculation ----- */
  }

} // adder::add()

